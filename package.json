{
  "name": "driveease-car-rental",
  "version": "1.0.0",
  "description": "DriveEase - Modern Car Rental & Fleet Management System",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server",
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start:all": "npm run install:all && npm run seed && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "playwright": "^1.62.1"
  }
}
